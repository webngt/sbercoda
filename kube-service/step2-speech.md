Итак давайте посмотрим, как работает тип сервиса ClusterIP. Это самый простой и самый базовый тип доступа к сервису. 

Созданный сервис имеет тип ClusterIP, это значит, что нашему сервису был назначен внутренний виртуальный IP. Узнать его мы можем из статуса сервиса с помощью команды kubectl describe.  

Давайте сделаем запрос на этот IP адрес, и убедимся, что приложение действительно на нем отвечает. Получить ClusterIP и сохранить его в переменной окружени можно с помощью формата вывода jsonpath команды kubectl get.   

Получим полную информацию об объекте Service в формате json

**Запускаем команду**

Смотрим, где именно в json находится информация с ClusterIP. И через jsonpath запрашиваем только его. 

**Запускаем команду.**

**Сохраняем в переменную окружения**:

**Проверим**, что сохранилось в переменную окружения:

Да, действительно, clusterIP cервиса. 

Теперь давайте обратимся к этому IP по 9000 порту с помощью команды curl. 

**Запустим команду.**

Видим ответ нашего приложения от одного из подов. 

Теперь давайте запустим в бесконечном цикле, и посмотрим, как будет происходить перераспределение запросов на поды. 

**Запускаем команду** 

Действительно получаем запросы от разных подов. 
Выйти из  цикла можно с помощью сочетания клавиш **Ctrl - C**

Итак мы посмотрели, как работает простейший сервис типа ClusterIP.